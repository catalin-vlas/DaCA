package com.wade.daca.matchalign.api;

public interface MatchAlignService {

    // TODO: Do something that actually makes sense, not this!
    public boolean match(String graph1, String graph2);
    public boolean align(String graph1, String graph2);

}
